services:
  - type: web
    name: erp-api
    env: docker
    dockerfilePath: ./apps/api/Dockerfile   # ← point here
    dockerContext: ./apps/api               # ← and here
    ...

  - type: web
    name: erp-web
    env: docker
    dockerfilePath: ./apps/web/Dockerfile
    dockerContext: ./apps/web
    ...